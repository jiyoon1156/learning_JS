const object = [{ name: "dog" }, { name: "cat" }];

object.push({ name: "horse" });

console.log(object.length);

const array = [1, true, { a: 1 }, [1, 2, 3, 4]];
array.push(6);

console.log(array.length);
